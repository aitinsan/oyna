import 'package:flutter/widgets.dart';
/// [GrammarCard] used to work with grammar tasks
class GrammarCard {
  String title;
  String text;
  GrammarCard({
    required this.title,
    required this.text,
  });
}
