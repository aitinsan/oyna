import 'package:flutter/widgets.dart';

class QuestionCard {
  String title;
  String topic;
  String text;
  QuestionCard({
    @required this.title,
    @required this.topic,
    @required this.text,
  });
}
